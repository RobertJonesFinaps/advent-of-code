/**
 * flips bits based on matches
 * @param first an integer
 * @param second an integer
 * @returns integer with bits flipped
 */
export function bitwiseOperationXOR(first: number, second: number): number {
  return first ^ second;
}
