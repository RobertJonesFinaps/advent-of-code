export interface MachineLightsConfig {
  target: number; // binary representation of target state
  operations: number[]; // binary representation of operations
  space: number; // Size of state space
}
